
// Rendered at container start via envsubst.
// Keep as a separate file so you can change envs without rebuilding the app.
window.__APP_CONFIG = {
  ORTHANC_BASE: "${ORTHANC_BASE}",
  OHIF_ROOT: "${OHIF_ROOT}"
};
